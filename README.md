# CORS
HFS plugin enabling CORS requests
allows for regex/path filtering on cors type requests only

Do not install if you want to allow all cors, in that case use rejetto/cors.

HFS ~ HTTP File Server https://github.com/rejetto/hfs
